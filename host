[master] # dockerized gitlab and jenkins

[vm] # slave server spawning docker containers

[build] # builds AMI's and Dockerfiles, uploads to S3

[db0] # volume container for gitlab and jenkins

[db1] # db for app

[local] # vagrant VM that shares folders locally
localhost
